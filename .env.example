# Database connection
DB_HOST=127.0.0.1
DB_NAME=monitoring_db
DB_USER=monitor_user
DB_PASS=change_me
DB_PORT=5432

# Database schema used by all agents
DB_SCHEMA=monitoring

# Optional runtime settings for run_monitoring.sh (when exported in shell/cron)
# PHASE_TIMEOUT_SECONDS=180
# LOG_RETENTION_DAYS=30

# Gateway authentication monitor
GATEWAY_USER=admin
GATEWAY_PASS=admin_password
# GATEWAY_CHECK_URL=https://www.google.com
# GATEWAY_LOG_FILE=/var/log/printer_test/gateway_auth_monitor.log